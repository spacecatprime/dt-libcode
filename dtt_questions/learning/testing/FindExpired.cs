using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace testing
{
    /// <summary>
    /// build a graph of nodes with an expire time
    /// go through the graph finding the expired nodes
    /// remove expired nodes from graph
    /// </summary>
    [TestClass]
    public class FindExpired
    {
        [TestMethod]
        public void DoFindExpired()
        {
        }
    }
}
